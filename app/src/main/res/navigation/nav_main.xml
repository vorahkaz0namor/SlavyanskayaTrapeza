<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_main"
    app:startDestination="@id/startFragment">

    <fragment
        android:id="@+id/startFragment"
        android:name="ru.slavyanskaya.trapeza.activity.StartFragment"
        tools:layout="@layout/fragment_start">
        <!--<action
            android:id="@+id/action_startFragment_to_menuFragment"
            app:destination="@id/menuFragment"/>
        <action
            android:id="@+id/action_startFragment_to_orderFragment"
            app:destination="@id/orderFragment"/>-->
    </fragment>

    <fragment
        android:id="@+id/menuFragment"
        android:name="ru.slavyanskaya.trapeza.activity.MenuFragment"
        tools:layout="@layout/fragment_menu">
        <!--<action
            android:id="@+id/action_menuFragment_to_orderFragment"
            app:destination="@id/orderFragment"/>-->
    </fragment>

    <fragment
        android:id="@+id/orderFragment"
        android:name="ru.slavyanskaya.trapeza.activity.OrderFragment"
        tools:layout="@layout/fragment_order"/>

</navigation>